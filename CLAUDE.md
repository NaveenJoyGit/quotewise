This project builds QuoteWise — an AI-powered WhatsApp quotation tool for interior contractors in India. Always follow the architectural decisions in SPEC.md, treating 'tight' sections as binding and 'loose' sections as guidance. Before implementing any feature, produce a plan and wait for my approval. All Python code uses Python 3.11+. Tests must pass before any code is committed. For every LLM call, keep the prompt in a Jinja template under app/prompts/, not inline in code. Log every LLM call with prompt template name, token count, and latency.
